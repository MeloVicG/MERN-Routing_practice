import React from "react";

const PageThree= (props) => {

    return (
        <div>
            <h3>this is page {props.id}</h3>
            <h4>place an number or word in the :id in the URL</h4>
        </div>
    )
}

export default PageThree;
